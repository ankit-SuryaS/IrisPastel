title: "[Feat✨] "
labels: []
body:
  # - type: markdown
  #   attributes:
  #     value: |
  #       This is text that will show up in the template!
  - type: textarea
    id: description
    attributes:
      label: Describe the feature that you would like added
      description: "Include the expected behaviour of the feature."
    validations:
      required: true
  - type: markdown
    attributes:
      value: |
        ## Markdown header
        And some more markdown
  - type: input
    id: has-id
    attributes:
      label: Suggestions
      description: A description about suggestions to help you
    validations:
      required: true
  - type: dropdown
    id: priority
    attributes:
      label: Request priority
      options:
        - High
        - Normal
        - Low
  - type: checkboxes
    attributes:
      label: Validations
      description: Before submitting the request, please make sure you do the following.
      options:
        - label: I have checked that there isn't already a request for the same feature to avoid creating a duplicate.
          required: true
